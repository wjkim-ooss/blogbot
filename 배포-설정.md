# 블로그봇 배포 · 회원 설정 가이드

지금은 **관리자 단독 로컬 모드**로 우진님만 씁니다(로그인 없음). 아래를 설정하면 로그인·승인·등급이 켜지고, 원장들에게 배포할 수 있습니다.

## 등급 정책
- **관리자(우진)**: 초안 생성 **무제한**, 회원 승인·등급 변경, 모든 기능.
- **원장(1단계)**: 승인되면 레퍼런스 열람 + 초안 작성. 초안 생성 **월 10회**(매달 1일 초기화). 초안은 각자 개인 폴더에 저장(서로 안 보임).
- **승인 대기**: 가입만 하고 관리자 승인 전 → 아무것도 못 봄.

## 켜는 순서

### 1. Supabase 준비
skin-study에 쓰는 Supabase 프로젝트를 재사용하거나 새 프로젝트를 만듭니다.

### 2. 회원 테이블 생성
Supabase 대시보드 → **SQL Editor** → `supabase_setup.sql` 내용을 붙여넣고 **Run**.
(profiles 테이블 + 가입 시 프로필 자동생성 트리거가 만들어집니다.)

### 3. 키 3개를 .env에 넣기
Supabase → **Project Settings → API** 에서 복사:
- Project URL → `SUPABASE_URL`
- `anon` `public` key → `SUPABASE_ANON_KEY`
- `service_role` key(비밀) → `SUPABASE_SERVICE_ROLE_KEY`

`블로그봇/.env` 파일에 세 줄을 채우고 서버 재시작(`node server.mjs`).
서버 로그에 **"인증 ON"** 이 뜨면 성공.

### 4. 관리자(우진) 계정 만들기
- 사이트 접속 → **회원가입**(우진님 이메일: kwjin4039@gmail.com)으로 가입.
- Supabase SQL Editor에서 한 줄 실행해 관리자로 승격:
  ```sql
  update public.profiles set role='admin', status='approved' where email='kwjin4039@gmail.com';
  ```
- 다시 로그인하면 "회원 관리" 탭이 보이고 전부 사용 가능.

### 5. 원장 초대·승인
- 원장에게 사이트 링크 전달 → 원장이 **회원가입 → 로그인** → "승인 대기" 화면.
- 우진님이 **회원 관리** 탭에서 해당 원장 **승인** 클릭 → 그때부터 원장이 레퍼런스·초안 사용.
- 등급·월 한도는 회원 관리 탭에서 원장별로 조정 가능.

## 참고
- **이메일 확인**: Supabase 기본값은 가입 시 이메일 인증을 요구합니다. 원장 가입을 간편하게 하려면 Supabase → Authentication → Providers → Email 에서 "Confirm email"을 끄면 됩니다(끄면 가입 즉시 로그인). 켜두면 원장이 메일 링크를 눌러야 로그인됩니다.
- **비용**: 원장이 AI 생성을 누르면 서버의 `ANTHROPIC_API_KEY`(=우진님 키)로 돌아가 우진님이 비용을 냅니다. 그래서 월 10회 한도가 곧 원장 1명당 월 최대 비용(대략 $1~2)입니다.
- **실제 공개 배포**: 지금 서버는 이 맥에서만 열립니다(localhost). 원장들이 외부에서 접속하려면 24시간 도는 호스팅(예: Railway/Render/VPS)에 올려야 합니다. 코드·인증은 배포 준비가 끝난 상태이니, 배포할 때 호스팅만 붙이면 됩니다.
