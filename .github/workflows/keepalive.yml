name: keep-alive

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: wake
        run: |
          curl -s -o /dev/null -w "%{http_code}\n" --max-time 120 https://blogbot-u7s9.onrender.com/
          exit 0
