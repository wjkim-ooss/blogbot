# 블로그봇 배포 가이드 (Render + Supabase)

원장들에게 배포하는 전체 순서입니다. 코드·설정은 준비 완료 상태예요.
아래 A→B→C→D 순서로 따라오시면 됩니다. 막히면 저(채팅)한테 물어보세요.

## 등급 정책 (이미 코드에 반영됨)
- **관리자(우진)**: 초안 생성 무제한, 회원 승인·등급 변경, 전부 가능.
- **원장(1단계)**: 승인되면 레퍼런스 열람 + 초안 작성. 생성 **월 10회**(매달 초기화). 초안은 각자 개인 소유(서로 안 보임).
- **승인 대기**: 가입만 하고 승인 전 → 아무것도 못 봄.

---

## A. Supabase 준비 (회원·초안 저장소)

skin-study에 쓰는 Supabase 프로젝트를 재사용하거나 새 프로젝트를 만듭니다.

1. Supabase 대시보드 → **SQL Editor** → `supabase_setup.sql` 전체를 붙여넣고 **Run**.
   (profiles·drafts 테이블 + 가입 시 프로필 자동생성 트리거가 만들어집니다.)
2. **Project Settings → API** 에서 3개 값을 메모:
   - Project URL → `SUPABASE_URL`
   - `anon` `public` key → `SUPABASE_ANON_KEY`
   - `service_role` key (비밀) → `SUPABASE_SERVICE_ROLE_KEY`

---

## B. GitHub에 코드 올리기 (가장 쉬운 방법: GitHub Desktop)

Render는 GitHub 저장소에서 배포합니다. 코드는 이미 커밋되어 있으니 올리기만 하면 됩니다.

1. **GitHub 계정**이 없으면 github.com 에서 가입.
2. **GitHub Desktop**(desktop.github.com) 설치·로그인.
3. `File → Add local repository` → 폴더 선택: **`/Users/woojinkim/우진 스레드/블로그봇`**
   (이미 git 저장소로 초기화해 뒀습니다.)
4. 오른쪽 위 **Publish repository** 클릭 → **Keep this code private 체크(중요)** → Publish.
   → GitHub에 `블로그봇`(또는 원하는 이름) 비공개 저장소가 생깁니다.

> 명령줄이 편하면: 깃허브에서 빈 private repo 생성 후, 블로그봇 폴더에서
> `git remote add origin <저장소주소> && git push -u origin main`

---

## C. Render 배포

1. **render.com** 가입 (Sign up with GitHub 로 하면 연결이 쉬움).
2. 대시보드 → **New +** → **Blueprint** 선택 → 방금 만든 저장소 연결.
   (저장소의 `render.yaml`을 자동으로 읽어 웹 서비스를 만듭니다. Blueprint가 안 보이면 **New + → Web Service**로 저장소를 골라도 됩니다. Build: `npm install`, Start: `npm start`.)
3. 배포 전에 **환경변수(Environment)** 4개를 입력:
   - `ANTHROPIC_API_KEY` = (Claude API 키)
   - `SUPABASE_URL` = (A-2에서 메모)
   - `SUPABASE_ANON_KEY` = (A-2)
   - `SUPABASE_SERVICE_ROLE_KEY` = (A-2)
4. **Create / Deploy** → 몇 분 뒤 **공개 주소**(예: `https://blogbot-xxxx.onrender.com`)가 나옵니다.
   서버 로그에 **"인증 ON"** 이 뜨면 성공.

> 무료 플랜은 15분 미사용 시 잠들었다가 다음 접속 때 30초쯤 깨어납니다.
> 항상 즉시 열리게 하려면 Render에서 유료(월 $7)로 올리면 됩니다.

---

## D. 관리자 계정 + 원장 초대

1. **Supabase → Authentication → URL Configuration → Site URL** 을 Render 주소
   (`https://blogbot-xxxx.onrender.com`)로 설정. (이메일 확인 링크가 이 주소로 갑니다.)
2. 배포된 사이트 접속 → **회원가입**(우진님 이메일: kwjin4039@gmail.com).
3. Supabase → SQL Editor 에서 관리자 승격 한 줄 실행:
   ```sql
   update public.profiles set role='admin', status='approved' where email='kwjin4039@gmail.com';
   ```
   → 다시 로그인하면 "회원 관리" 탭이 보이고 전부 사용 가능.
4. 원장에게 사이트 링크 전달 → 원장이 **회원가입 → 로그인** → "승인 대기" 화면.
5. 우진님이 **회원 관리** 탭에서 **승인** 클릭 → 그때부터 원장이 레퍼런스·초안 사용.

---

## 참고
- **이메일 확인**: Supabase 기본값은 가입 시 이메일 인증을 요구합니다. 원장 가입을 간편하게 하려면
  Supabase → Authentication → Providers → Email 에서 "Confirm email"을 끄면 됩니다(끄면 가입 즉시 로그인).
  켜두면 원장이 메일 링크를 눌러야 로그인됩니다(그래서 D-1의 Site URL 설정이 중요).
- **비용**: 원장이 AI 생성을 누르면 서버의 `ANTHROPIC_API_KEY`(=우진님 키)로 돌아가 우진님이 냅니다.
  월 10회 한도가 곧 원장 1명당 월 최대 비용(대략 $1~2)입니다. 회원 관리 탭에서 원장별로 조정 가능.
- **레퍼런스 갱신**: 새 레퍼런스는 채팅에서 저한테 부탁하시면 수집됩니다. 배포본에 반영하려면
  그 파일을 GitHub에 올리면(GitHub Desktop에서 Commit → Push) Render가 자동 재배포합니다.
- **로컬 사용**: `.env`에 Supabase 키를 안 넣으면 지금처럼 이 맥에서 관리자 단독으로 씁니다(로그인 없음).
